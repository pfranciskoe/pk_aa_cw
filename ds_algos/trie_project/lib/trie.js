class Node {
    constructor() {
        this.children = {};
        this.isTerminal = false;
    }
}

class Trie {
   constructor(node){
       this.root = new Node();
   }
   insertRecur(word,root=this.root){
       if this
   }
}

module.exports = {
    Node,
    Trie
};